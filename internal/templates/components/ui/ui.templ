package ui

templ H2(txt string) {
	<h2 class="text-2xl font-bold mb-6 text-gray-800">{ txt }</h2>
}

templ H3(txt string) {
	<h3 class="text-xl font-semibold mb-4 text-gray-700">{ txt }</h3>
}

templ Line() {
	<div class="border-t border-gray-200 my-4"></div>
}